/*The Connection String To The DB*/
const dbKey = {
    host: "localhost",
    user: "root",
    password: "",
    database: "ideator"
};
//The Prefix Of the DB
const tabKey = ""
//The key value pairs that help in matching requests to the tables Of the DB
const tables = {
    user: tabKey + 'users',
    workspace: tabKey + 'workspaces',
    team: tabKey + 'teams',
    project: tabKey + 'projects',
    idea: tabKey + 'ideas',
    message: tabKey + 'messages',
    feedback: tabKey + 'feedback',
    workList: tabKey + 'worklist',
    messageList: tabKey + 'messagelist',
    projectList: tabKey + 'projectlist',
    ideaList: tabKey + 'idealist'
}
module.exports = [dbKey, tables];
/*

ADD THIS FILE TO GITIGNORE
DO NOT COMMIT THIS FILE

*/
